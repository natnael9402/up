// Placeholder test to anchor the test directory.
// Feature: single-market-view-ui-ux

describe('testing infrastructure', () => {
  it('is set up correctly', () => {
    expect(1).toBe(1);
  });
});
